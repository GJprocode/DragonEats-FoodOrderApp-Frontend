// src/utils/textFilter.ts

// import leoProfanity from 'leo-profanity';

// export const checkForBannedWords = (text: string): void => {
//   if (leoProfanity.isProfane(text)) {
//     throw new Error("Inappropriate content detected");
//   }
// };

